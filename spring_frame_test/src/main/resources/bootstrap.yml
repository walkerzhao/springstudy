eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry:
      false
    register-with-eureka:
      false
server:
  port: 8763
spring:
  application:
    name: service-frame_test

logging.level:
  root:
    DEBUG

server.tomcat.max-http-header-size: 3145728

spring.aop.auto: true
