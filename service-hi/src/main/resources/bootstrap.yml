eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry:
      false
    register-with-eureka:
      false
server:
  port: 8763
spring:
  application:
    name: service-hi

logging.level:
  root:
    DEBUG